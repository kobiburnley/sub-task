import {tasks} from "../src/sprint"
import {Task} from "../src/task"

require("chai").should()

describe("task.test", function () {
  it("should", () => {
    console.log("Add report here", tasks)
  })
})
